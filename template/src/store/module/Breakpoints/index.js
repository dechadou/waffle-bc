import Breakpoints, { SET_BREAKPOINT } from './Breakpoints';

export const BreakpointsNamespace = 'Breakpoints';

export const BreakpointsMutationTypes = {
  SET_BREAKPOINT: `${BreakpointsNamespace}/${SET_BREAKPOINT}`,
};

export default Breakpoints;
